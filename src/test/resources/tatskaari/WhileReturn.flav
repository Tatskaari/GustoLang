function whileTest() do
    val a : integer := 10
    val b : integer := 0
    while a <= 10 do
        b := b + a
        if b >= 20 then
            return 100
        end
    end
end

val out : integer := whileTest()