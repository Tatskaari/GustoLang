function whileTest() do
    integer a := 10
    integer b := 0
    while a <= 10 do
        b := b + a
        if b >= 20 then
            return 100
        end
    end
end

integer out := whileTest()